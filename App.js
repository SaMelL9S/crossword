import { StatusBar, TouchableOpacity, Text, Image, View, Alert } from 'react-native';
import styled from 'styled-components/native';
import AppNavigator from './app.navigator';
import Crossvord from './screens/crossvord';

const App = () => {
  return (
      <Crossvord/>
  )
}

export default App;